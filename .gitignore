# Local Broker Bridge configuration is routing/identity metadata, never a vendor secret store.
broker.env
broker.env.*

# Local application settings and generated outputs.
.env
.env.*
!.env.example
node_modules/
dist/
coverage/
bin/
obj/
__pycache__/
*.py[cod]

# Local coordination files are intentionally not product documentation.
AGENT.md
conflicts.md

# Directive-framework runtime state (local, never product docs)
_directives/

# Local agent/tool state — never product content.
.claude/
.codex/
.agents/
CLAUDE.md
*.log
nohup.out

# Historical material, kept locally only.
_archive/
brainstorm/

# Development-only material is maintained outside the public distribution repository.
**/test/
**/tests/
demo/
examples/

# park: local "where we left off" notes (not committed)
.park/

# Generated deployment state contains resource identifiers for one instance.
# It is never source-controlled or used as an input for another tenant.
.tessera/
handoffs/

# Local Azure deployment plans, operation records, and generated diagnostics.
.azure/
