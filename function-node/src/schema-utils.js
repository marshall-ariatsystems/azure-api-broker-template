'use strict';

// Compatibility import for existing Function modules; new code imports sdk directly.
const { requireObject, rejectUnknownKeys } = require('../../sdk');
module.exports = { requireObject, rejectUnknownKeys };
