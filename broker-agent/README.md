# Broker agent

The future home of the portable `broker-bridge` executable: loopback proxy, application lifecycle,
profile loading, credential-placeholder filtering, diagnostics, and release verification.

The working Azure reference stays in [`../clients/bridge`](../clients/bridge) until its stable
contracts and packaging tests have been extracted. This is a boundary marker, not a second implementation.
