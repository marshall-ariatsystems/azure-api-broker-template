{
  "$comment": "A5 — GitHub OIDC federated identity credential (spec §4.4). Issuer MUST have NO trailing slash (the council's exactness fix); matching is exact+case-sensitive. Audience == api://AzureADTokenExchange. Subject scoped to repo:ORG/REPO:environment:<env> (no wildcard org). The workload SP gets a DIRECT per-key broker app-role assignment (no group nesting) per grant-revoke-runbook.md §4.",
  "name": "broker-ci-fic",
  "issuer": "https://token.actions.githubusercontent.com",
  "subject": "repo:ORG/REPO:environment:production",
  "audiences": ["api://AzureADTokenExchange"],
  "description": "Broker CI workload identity federation for ORG/REPO production environment. NO trailing slash on issuer."
}
