# Broker Bridge configuration — no vendor API key belongs in this file.
# Generate this file with `npm run init -- --preset openai`, then replace the placeholders
# with values from generated local deployment state.
BROKER_BASE=https://<broker-host>/api/broker
BROKER_SCOPE=api://<broker-app-id>/.default
BROKER_VENDOR=generic
