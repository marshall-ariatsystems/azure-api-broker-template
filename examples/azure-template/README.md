# Azure template example

The existing deployment walkthrough and demonstration remain in `docs/`, `demo/`, `cicd/`, and the
Azure provider paths. This marker makes their role explicit: an operational reference deployment,
not the product-core layout.
